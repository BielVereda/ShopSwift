import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    section: {
        backgroundColor: '#ffffff',
        borderRadius: 8,
        padding: 15,
        marginVertical: 10,
        borderWidth: 1,
        borderColor: '#e0e0e0',
    },
    heading: {
        fontSize: 18,
        fontWeight: '600',
        color: '#6200ee',
        marginBottom: 5,
    },
    description: {
        fontSize: 14,
        color: '#666666',
        lineHeight: 20,
    },
});
